{
  "scripts": {
    "build:packs": "node ./utils/packs.mjs package pack",
    "build:unpack": "node ./utils/packs.mjs package unpack",
    "git:reset-content-sync": "git branch -D content-sync 2>/dev/null || true; git push origin --delete content-sync 2>/dev/null || true; git checkout release; git checkout -b content-sync; git push -u origin content-sync; git checkout release"
  },
  "dependencies": {
    "@babel/eslint-parser": "^7.22.15",
    "@foundryvtt/foundryvtt-cli": "^3.0.3",
    "@rollup/plugin-node-resolve": "^15.2.1",
    "eslint-plugin-jsdoc": "^46.5.0",
    "fancy-log": "^2.0.0",
    "less": "^4.6.4",
    "rollup": "^3.29.5",
    "yargs": "^17.7.2"
  }
}
